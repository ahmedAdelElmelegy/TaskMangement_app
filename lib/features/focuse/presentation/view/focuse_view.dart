import 'package:flutter/material.dart';

class FocuseView extends StatelessWidget {
  const FocuseView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
